# Inpainting is handled by ImageGenerator.inpaint() via the /api/inpaint
# endpoint in routes.py. This module is reserved for future non-inpaint
# editing operations (e.g. outpainting, style transfer, video editing).
